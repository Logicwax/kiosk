// Renderer entry. Importing the stylesheet here is what puts it through webpack
// (MiniCssExtractPlugin emits it as a real .css file, which HtmlWebpackPlugin
// then links into the generated index.html).
import './style.css'
